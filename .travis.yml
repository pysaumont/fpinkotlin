language: java
 
sudo: required

jdk:
   - openjdk8

install: true

script:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - java -version
  - ./fpinkotlin-parent/gradlew

notifications:
  email:
    recipients:
      - build@volga.fr
    on_success: always # default: change [always|never|change]
    on_failure: always # default: always [always|never|change]
